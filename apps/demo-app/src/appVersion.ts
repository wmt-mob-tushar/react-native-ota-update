// ─────────────────────────────────────────────────────────────
// Bump these on every OTA release so the change is visible on screen.
// This file ships INSIDE the JS bundle, so it updates over the air.
// ─────────────────────────────────────────────────────────────
export const APP_VERSION = 'v2';
export const APP_TAGLINE = '🚀 Updated over the air — no reinstall!';
export const APP_ACCENT  = '#059669'; // green
